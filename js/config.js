// Site-wide configuration. Edit values here — no other file needs changing.
window.SITE_CONFIG = {
  // Where "Inquire" and contact form messages are sent.
  // NOTE: the first message sent through the form triggers a one-time
  // activation email from formsubmit.co to this address — click the link
  // in it once and all later messages arrive normally.
  inquiryEmail: "ahir.valluri@gmail.com",

  siteName: "Lathika Arts",
  tagline: "Hand-sculpted clay florals, panels & vessels",
};
